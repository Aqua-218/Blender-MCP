{
	"servers": {
		"blenderMcp": {
			"type": "stdio",
			"command": "${workspaceFolder}/.venv/bin/python",
			"args": [
				"-m",
				"mcp_server.main",
				"--transport",
				"stdio"
			],
			"envFile": "${workspaceFolder}/.env"
		}
	}
}